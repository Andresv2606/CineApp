<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fondo -->
    <solid android:color="#FFFFFF" />

    <!-- Bordes del EditText -->
    <stroke
        android:width="2dp"
        android:color="#D32F2F" />   <!-- Color rojo Cinemax -->

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />

    <!-- Padding interior -->
    <padding
        android:left="12dp"
        android:top="8dp"
        android:right="12dp"
        android:bottom="8dp" />

</shape>
